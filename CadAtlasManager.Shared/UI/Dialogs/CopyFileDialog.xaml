<Window x:Class="CadAtlasManager.CopyFileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="复制并打开" Height="220" Width="450" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Name="TxtSourceInfo" Text="原文件：" Foreground="Gray" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="重命名：" VerticalAlignment="Center"/>
            <TextBox Name="TbNewName" Grid.Column="1" Height="26" VerticalContentAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="保存路径：" VerticalAlignment="Center"/>
            <TextBox Name="TbSavePath" Grid.Column="1" Height="26" VerticalContentAlignment="Center" IsReadOnly="True" Background="#f9f9f9"/>
            <Button Content="..." Grid.Column="2" Click="BtnBrowse_Click" Margin="5,0,0,0"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="确定复制并打开" Width="120" Height="30" Click="BtnOK_Click" Background="#007ACC" Foreground="White"/>
            <Button Content="取消" Width="80" Height="30" Margin="10,0,0,0" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>